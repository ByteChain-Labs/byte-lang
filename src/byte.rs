use std::env;
use std::fs;
use std::io{self, Write};

struct Byte;

impl Byte {
  fn new() -> Self {
    Byte
  }

  fn run_file() {
    println!('')
  }

  fn run_prompt() {
    
  }

  fn run() {
    
  }
}
